{
  "name": "authentication",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "authentication/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "authentication"
      },
      "dependsOn": ["@lhg/utils:build"]
    },
    "start:dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run start:dev",
        "cwd": "authentication"
      }
    }
  },
  "tags": ["app", "backend"]
}
